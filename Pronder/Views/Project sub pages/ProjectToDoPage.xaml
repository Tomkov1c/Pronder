<Page
    x:Class="Pronder.Views.ProjectToDoPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:ui="using:CommunityToolkit.WinUI"
    xmlns:data="using:Pronder.Models"
    mc:Ignorable="d">

    <ScrollViewer>
        <StackPanel Orientation="Vertical" HorizontalAlignment="Stretch">
            <Grid HorizontalAlignment="Stretch">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="auto" />
                </Grid.ColumnDefinitions>
                <controls:Segmented  SelectedIndex="0" SelectionMode="Single" HorizontalAlignment="Left" Grid.Column="0">
                    <controls:SegmentedItem Icon="{ui:FontIcon Glyph=&#xEA3A;}" IsSelected="True"/>
                    <controls:SegmentedItem Icon="{ui:FontIcon Glyph=&#xE930;}" />
                </controls:Segmented>
                <Button HorizontalAlignment="Right" Grid.Column="2">
                    <Button.Content>
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE710;" FontSize="18"/>
                            <TextBlock Text="Add" />
                        </StackPanel>
                    </Button.Content>
                    <Button.Flyout>
                        <Flyout Placement="BottomEdgeAlignedRight">
                            <StackPanel Orientation="Horizontal" Spacing="12">
                                <TextBox  Width="300" x:Name="NewTaskContentTextBox"/>
                                <Button Content="Add" Style="{StaticResource AccentButtonStyle}">
                                    <Button.KeyboardAccelerators>
                                        <KeyboardAccelerator Key="Enter" />
                                    </Button.KeyboardAccelerators>
                                </Button>
                            </StackPanel>
                        </Flyout>
                    </Button.Flyout>
                </Button>
            </Grid>

            <ListView x:Name="MyListView" SelectionMode="None" ItemsSource="{Binding Tasks}" Margin="0, 24, 0, 0">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Vertical" Padding="24, 0" Margin="-16, 1" HorizontalAlignment="Stretch" BorderThickness="0, 0, 0, 1" 
                                    BorderBrush="{ThemeResource ControlFillColorSecondaryBrush}" CornerRadius="0">

                            <StackPanel Orientation="Horizontal" Padding="0, 14" Spacing="4">
                                <CheckBox CornerRadius="12" MinWidth="0" IsChecked="{Binding Done}" />
                                <TextBlock VerticalAlignment="Center" HorizontalAlignment="Stretch" Text="{Binding Content}" />
                            </StackPanel>

                            <ListView ItemsSource="{Binding SubTasks}" Margin="16,0,0,0" SelectionMode="None">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Vertical" Padding="0, 0, 0, 16">
                                            <StackPanel Orientation="Horizontal" Padding="4, 0" Spacing="4" HorizontalAlignment="Stretch">
                                                <CheckBox CornerRadius="12" MinWidth="0" IsChecked="{Binding Done}" />
                                                <TextBlock VerticalAlignment="Center" HorizontalAlignment="Stretch" Text="{Binding Content}" />
                                            </StackPanel>

                                        </StackPanel>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>


        </StackPanel>
    </ScrollViewer>
    
</Page>
