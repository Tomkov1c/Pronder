<Page
    x:Class="Pronder.Views.ShellPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:helpers="using:Pronder.Helpers"
    xmlns:behaviors="using:Pronder.Behaviors"
    xmlns:i="using:Microsoft.Xaml.Interactivity"
    xmlns:local="using:Pronder.ViewModels"
    xmlns:localviews="using:Pronder.Views"
    x:Name="ShellPageName">

    <Page.Resources>
        <DataTemplate x:Key="GlyphTemplate" x:DataType="local:Pages">
            <NavigationViewItem Content="{x:Bind Name}" Icon="{x:Bind Icon}" />
        </DataTemplate>

        <DataTemplate x:Key="SeparatorTemplate">
            <NavigationViewItemSeparator/>
        </DataTemplate>

        <DataTemplate x:Key="IconTemplate" x:DataType="local:ProjectBrief">
            <NavigationViewItem Content="{x:Bind Name}" Tag="{x:Bind ProjectPath}">
                <NavigationViewItem.Icon>
                    <ImageIcon Source="{x:Bind Icon}" />
                </NavigationViewItem.Icon>
            </NavigationViewItem>
        </DataTemplate>

        <localviews:ItemTemplateSelector x:Key="ItemTemplateSelector"
                                         GlyphTemplate="{StaticResource GlyphTemplate}"
                                         IconTemplate="{StaticResource IconTemplate}" 
                                         SeparatorTemplate ="{StaticResource SeparatorTemplate}"/>
    </Page.Resources>

    <Grid>
        <Grid Canvas.ZIndex="1" Height="{Binding ElementName=NavigationViewControl, Path=CompactPaneLength}"
              HorizontalAlignment="Stretch" IsHitTestVisible="True" VerticalAlignment="Top"
              x:Name="AppTitleBarParent" >
            <StackPanel Orientation="Horizontal" Grid.Column="0" Margin="48, 0, 0, 0">
                <Button Padding="8" Width="32" Height="32" IsTabStop="False" x:Name="AppTitleBarButton"
                        Background="{ThemeResource ControlFillColorTransparentBrush}"
                        BorderBrush="{ThemeResource ControlFillColorTransparentBrush}">
                    <Button.Content>
                        <ImageIcon Source="ms-appx:///Assets/Icon.png" />
                    </Button.Content>
                    <Button.Flyout>
                        <MenuFlyout Placement="BottomEdgeAlignedLeft">
                            <MenuFlyoutItem Text="New project" Width="250" Icon="Add" ToolTipService.ToolTip="Create a new project"
                                            Command="{Binding NavigateToPageCommand}" CommandParameter="NewProjectPage">
                                <MenuFlyoutItem.KeyboardAccelerators>
                                    <KeyboardAccelerator Modifiers="Control, Shift" Key="N"/>
                                </MenuFlyoutItem.KeyboardAccelerators>
                            </MenuFlyoutItem>
                            
                            <!--<MenuFlyoutSeparator />
                            
                            <MenuFlyoutItem Text="About" ToolTipService.ToolTip="About the app."
                                            Command="{Binding NavigateToPageCommand}" CommandParameter="AboutPage">
                                <MenuFlyoutItem.Icon>
                                    <FontIcon Glyph="&#xE946;" />
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>
                            
                            <MenuFlyoutItem Text="Help" Click="ActivatePageHelp" Icon="Help" 
                                            ToolTipService.ToolTip="Get more inforamtion about the current page.">
                                <MenuFlyoutItem.KeyboardAccelerators>
                                    <KeyboardAccelerator Key="F1"/>
                                </MenuFlyoutItem.KeyboardAccelerators>
                            </MenuFlyoutItem>-->
                            
                            <MenuFlyoutSeparator />

                            <MenuFlyoutItem Text="Settings" Icon="Setting" ToolTipService.ToolTip="Open settings"
                                            Command="{Binding NavigateToPageCommand}" CommandParameter="SettingsPage"> 
                                <MenuFlyoutItem.KeyboardAccelerators>
                                    <KeyboardAccelerator Key="F12"/>
                                </MenuFlyoutItem.KeyboardAccelerators>
                            </MenuFlyoutItem>
                        </MenuFlyout>
                    </Button.Flyout>
                </Button>
                <Grid x:Name="AppTitleBar" Width="10000" Height="48">
                    <TextBlock x:Name="AppTitleBarText" VerticalAlignment="Center" HorizontalAlignment="Stretch"
                               Style="{StaticResource CaptionTextBlockStyle}" Margin="0,0,0,0"  Grid.Column="1" 
                               Text="Pronder"/>
                </Grid>
            </StackPanel>

        </Grid>
        <NavigationView x:Name="NavigationViewControl" SelectionChanged="ItemClicked" Canvas.ZIndex="0" 
                        IsBackButtonVisible="Collapsed" SelectedItem="{x:Bind _viewModel.Selected, Mode=OneWay}" 
                        IsSettingsVisible="False" ExpandedModeThresholdWidth="1280" 
                        MenuItemTemplateSelector="{StaticResource ItemTemplateSelector}"
                        IsFocusEngaged="False">
            <NavigationView.Transitions>
                <TransitionCollection>
                    <EntranceThemeTransition IsStaggeringEnabled="True" />
                </TransitionCollection>
            </NavigationView.Transitions>

            <NavigationView.MenuItemsSource>
                <Binding Path="PaneItems" Mode="OneWay"/>
            </NavigationView.MenuItemsSource>
            
            <ScrollView Margin="64, 0, 0, 0" >
                <Frame IsNavigationStackEnabled="False" x:Name="NavigationFrame" Padding="0, 52, 64, 50" CornerRadius="{StaticResource ControlCornerRadius}"/>
            </ScrollView>
        </NavigationView>
    </Grid>
</Page>
