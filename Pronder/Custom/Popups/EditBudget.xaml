<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="Pronder.Custom.EditBudgetPopup"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Pronder.Custom"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:ui="using:CommunityToolkit.WinUI"
    CloseButtonText="Close"
    Style="{StaticResource DefaultContentDialogStyle}">
    <ContentDialog.Resources>
        <ResourceDictionary>
            <x:Double x:Key="ContentDialogMaxWidth">700</x:Double>
            <x:Double x:Key="ContentDialogMaxHeight">550</x:Double>
            <x:Double x:Key="ContentDialogMinWidth">700</x:Double>
            <x:Double x:Key="ContentDialogMinHeight">550</x:Double>

        </ResourceDictionary>
    </ContentDialog.Resources>

    <StackPanel Orientation="Vertical" Spacing="12">
        <controls:Segmented HorizontalAlignment="Stretch" 
                            SelectedIndex="{Binding SelectedItemTypeIndex, Mode=TwoWay}">
            <controls:SegmentedItem Content="Expense" Icon="{ui:FontIcon Glyph=&#xE73C;}"/>
            <controls:SegmentedItem Content="Income" Icon="{ui:FontIcon Glyph=&#xE710;}"/>
        </controls:Segmented>
        <TextBox Text="{Binding Item.Name, Mode=TwoWay}" PlaceholderText="Name"/>
        <TextBox Text="{Binding Item.Description, Mode=TwoWay}" PlaceholderText="Description"/>
        <NumberBox Text="{Binding Item.Amount, Mode=TwoWay}" PlaceholderText="Amount"/>
    </StackPanel>

</ContentDialog>
