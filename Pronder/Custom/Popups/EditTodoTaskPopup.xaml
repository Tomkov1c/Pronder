<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="Pronder.Custom.EditTodoTaskPopup"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Pronder.Custom"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    CloseButtonText="Close"
    Style="{StaticResource DefaultContentDialogStyle}">
    <ContentDialog.Resources>
        <ResourceDictionary>
            <x:Double x:Key="ContentDialogMaxWidth">700</x:Double>
            <x:Double x:Key="ContentDialogMaxHeight">550</x:Double>
            <x:Double x:Key="ContentDialogMinWidth">700</x:Double>
            <x:Double x:Key="ContentDialogMinHeight">550</x:Double>

        </ResourceDictionary>
    </ContentDialog.Resources>

    <StackPanel Orientation="Vertical" Spacing="16">
        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="32" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <CheckBox CornerRadius="10" Grid.Column="0" VerticalAlignment="Top" 
              IsChecked="{Binding Task.Done, Mode=TwoWay}"/>
            <TextBox Text="{Binding Task.Content, Mode=TwoWay}" IsSpellCheckEnabled="True" 
                     TextWrapping="Wrap"
                     Grid.Column="1" HorizontalAlignment="Stretch"/>

        </Grid>
        <ScrollViewer>
            
        </ScrollViewer>
    </StackPanel>
   
</ContentDialog>
