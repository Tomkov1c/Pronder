<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Pronder.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Pronder"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Pronder">
    
    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Row="0" Grid.Column="0">
            <TitleBar x:Name="AppTitleBar" x:Uid="AppName" IsBackButtonVisible="True" IsPaneToggleButtonVisible="True">
                <TitleBar.IconSource>
                    <ImageIconSource ImageSource="ms-appx:///Assets/Icon.png" />
                </TitleBar.IconSource>
                <TitleBar.Content>
                    <AutoSuggestBox MaxWidth="360" Width="360" MinWidth="100" HorizontalAlignment="Stretch" VerticalAlignment="Center" 
                                    PlaceholderText="Search.." QueryIcon="Find" />
                </TitleBar.Content>
            </TitleBar>
        </Grid>

        <Frame x:Name="MainWindowFrame" Grid.Row="1" Grid.Column="0"/>
    </Grid>
</Window>