<UserControl
    x:Class="Pronder.Custom.ExternalLinkListItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Pronder.Custom"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" Padding="16" HorizontalAlignment="Stretch"
          CornerRadius="{StaticResource ControlCornerRadius}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
          BorderThickness="1" ColumnSpacing="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="24" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="32" />
        </Grid.ColumnDefinitions>
        <Image Source="{Binding Icon, RelativeSource={RelativeSource Mode=TemplatedParent}}" Grid.Column="0" />
        <TextBox Grid.Column="1" Text="{Binding Name, RelativeSource={RelativeSource Mode=TemplatedParent}}" />
        <TextBox Grid.Column="2" Text="{Binding Path, RelativeSource={RelativeSource Mode=TemplatedParent}}" />
        <Button Grid.Column="3" Content="..." />
    </Grid>
</UserControl>

